# Android root-stack artifacts

- KernelSU-Next Manager `v3.3.0` build `33214` and matching Android ARM64 `ksud`, published 2026-07-03 from `KernelSU-Next/KernelSU-Next`.
- Zygisk Next `v1.4.3` build `817-e815170`, published 2026-07-16 from `Dr-TSNG/ZygiskNext`.
- LSPosed `v1.9.2` build `7024` (Zygisk variant), published 2023-10-11 from `LSPosed/LSPosed`.

The KernelSU and Zygisk hashes match the SHA-256 digests published by the GitHub Releases API. The LSPosed release predates GitHub asset digests, so its locally calculated SHA-256 is recorded in `SHA256SUMS`.
